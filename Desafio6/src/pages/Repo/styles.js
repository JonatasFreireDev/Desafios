import styled from 'styled-components/native';
import { ActivityIndicator } from 'react-native';

export const ActivityIndicatorMine = styled(ActivityIndicator).attrs({
    size: 50,
    color: '#7159c1',
})`
    flex: 1;
    margin: 40% auto;
`;
